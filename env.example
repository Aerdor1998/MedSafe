# MedSafe - Variáveis de Ambiente
# IMPORTANTE: Copie este arquivo para .env e preencha com valores únicos!
# NUNCA commite o arquivo .env no Git!

# ============================================================================
# BACKEND
# ============================================================================
PORT=8000
DEBUG=false

# ============================================================================
# POSTGRESQL
# ============================================================================
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=medsafe
POSTGRES_USER=medsafe
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_RANDOM_PASSWORD

# Database URL (será construída automaticamente se não definida)
# DATABASE_URL=postgresql://user:pass@host:port/db

# ============================================================================
# OLLAMA - IA
# ============================================================================
OLLAMA_HOST=http://ollama:11434
OLLAMA_LLM=qwen2.5:7b
OLLAMA_VLM=qwen2.5vl:7b

# ============================================================================
# SEGURANÇA - GERAR VALORES ÚNICOS E LONGOS!
# ============================================================================
# Gere com: python3 -c 'import secrets; print(secrets.token_urlsafe(32))'

SECRET_KEY=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE_MIN_32_CHARS
JWT_SECRET=CHANGE_ME_GENERATE_WITH_SECRETS_MODULE_MIN_32_CHARS
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# ============================================================================
# CORS - Listar domínios específicos separados por vírgula
# ============================================================================
# Desenvolvimento:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8050

# Produção (exemplo):
# ALLOWED_ORIGINS=https://medsafe.com,https://app.medsafe.com

# ============================================================================
# REDIS (para rate limiting e cache)
# ============================================================================
REDIS_URL=redis://redis:6379/0

# ============================================================================
# LOGS E MONITORAMENTO
# ============================================================================
LOG_LEVEL=INFO
LOG_FORMAT=json

# ============================================================================
# UPLOAD DE ARQUIVOS
# ============================================================================
MAX_UPLOAD_SIZE=10485760
ALLOWED_EXTENSIONS=jpg,jpeg,png,pdf

# ============================================================================
# APIs EXTERNAS
# ============================================================================
ENABLE_RXNORM=true
RXNORM_BASE_URL=https://rxnav.nlm.nih.gov/REST

# ============================================================================
# TELEMETRIA
# ============================================================================
ENABLE_METRICS=true
METRICS_PORT=9090

# ============================================================================
# INSTRUÇÕES
# ============================================================================
# 1. Copie este arquivo: cp env.example .env
# 2. Gere secrets seguros:
#    python3 -c 'import secrets; print(f"SECRET_KEY={secrets.token_urlsafe(32)}")'
#    python3 -c 'import secrets; print(f"JWT_SECRET={secrets.token_urlsafe(32)}")'
#    python3 -c 'import secrets; print(f"POSTGRES_PASSWORD={secrets.token_urlsafe(16)}")'
# 3. Substitua os valores CHANGE_ME acima
# 4. Configure ALLOWED_ORIGINS com seus domínios específicos
# 5. NUNCA commite o arquivo .env no Git!
